# SPDX-License-Identifier: GPL-2.0-only
#
# Makefile for industrial I/O LSM6DSOX driver
#

include Kbuild

ifeq ($(KERNELRELEASE),)

KBUILD_OPTIONS += CONFIG_IIO_ST_LSM6DSOX=m
KBUILD_OPTIONS += CONFIG_IIO_ST_LSM6DSOX_I2C=m
KBUILD_OPTIONS += CONFIG_IIO_ST_LSM6DSOX_SPI=m

ifneq ($(CONFIG_I3C),)
KBUILD_OPTIONS += CONFIG_IIO_ST_LSM6DSOX_I3C=m
endif

KBUILD_OPTIONS += CONFIG_IIO_ST_LSM6DSOX_ASYNC_HW_TIMESTAMP=n
KBUILD_OPTIONS += CONFIG_IIO_ST_LSM6DSOX_MLC_PRELOAD=n

modules:
	$(MAKE) -C $(KDIR) M=$(PWD) modules $(KBUILD_OPTIONS)

modules_install:
	$(MAKE) -C $(KDIR) M=$(PWD) modules_install

clean:
	$(MAKE) -C $(KDIR) M=$(PWD) clean
endif
